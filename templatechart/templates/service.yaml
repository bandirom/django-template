apiVersion: v1
kind: Service
metadata:
  name: {{ include "templatechart.fullname" . }}
  labels:
    {{- include "templatechart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "templatechart.selectorLabels" . | nindent 4 }}
