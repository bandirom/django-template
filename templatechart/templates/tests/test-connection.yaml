apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "templatechart.fullname" . }}-test-connection"
  labels:
    {{- include "templatechart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "templatechart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
